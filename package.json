{
  "name": "aliases.sh",
  "version": "1.0.0",
  "description": "My curated list of bash aliases that I use, hosted online so that I can access it easily from anywhere, especially on new devices. It's aimed mostly at developers using git and npm, but it has other useful aliases as well. Includes an alias concatenating the whole directory into a text file for LLM workflows.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24 <26"
  },
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "prebuild": "pnpm test",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "husky"
  },
  "author": "piotr@piech.dev",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/jsdom": "^28.0.1",
    "@types/node": "^25.5.2",
    "highlight.js": "^11.11.1",
    "husky": "^9.1.7",
    "jsdom": "^29.0.1",
    "typescript": "^6.0.2",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  }
}
